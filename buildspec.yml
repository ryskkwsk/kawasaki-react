version: 0.2

# CodeBuild

# BitBucketからS3にソースをアップロード

phases:
  install:
    runtime-versions:
      nodejs: 10

  pre_build:
    commands:
      - npm install yarn
      - yarn install

  build:
    commands:
      - yarn build

artifacts:
  files: [ '**/*' ]
  base-directory: build
